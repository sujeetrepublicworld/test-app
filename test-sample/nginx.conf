server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # Ensure ETag (304 ke liye zaroori)
    etag on;

    # 🔴 HTML – no cache (304 nahi aayega)
    location ~* \.html$ {
        add_header Cache-Control "no-store, no-cache, max-age=0, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    # 🟢 CSS / JS – cache + revalidation (304 AAYEGA)
    location ~* \.(css|js|png|jpg|jpeg|gif|svg|webp|ico|woff2?)$ {
        expires 0s;
        add_header Cache-Control "public, max-age=0" always;
        access_log off;
    }
}
